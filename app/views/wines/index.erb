<h1>All Wines</h1>

<h2> Welcome, <%= current_user.email%></h2>

<% @wines.each do |wine| %>
   <h3><%= wine.id%>. <a href= "wines/<%= wine.id%>"><%= wine.name %></h3>
   <h4> Added By: <%= wine.user.user_name%></h4>
    <% end %>


<br>#show user's wines
#option to edit wines 
#option to navigate to add new wine route



